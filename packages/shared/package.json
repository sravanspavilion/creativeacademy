{
  "name": "@academy/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types, schedule/time logic and seed data for the Creative Academy dashboard + API.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^5.0.1"
  }
}